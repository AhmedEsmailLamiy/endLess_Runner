using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ObstDin : MonoBehaviour
{
    // Start is called before the first frame update
    public float ObestcalFoece = 0.5f;
    private Vector3 no;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
        // if (this.gameObject.transform.position.x >= LevelBound.RightSideobs)
        //{
        //    new WaitForSeconds(20);
        //    //transform.Translate(Vector3.right * Time.deltaTime * ObestcalFoece);
        //    // no.x =  Time.deltaTime * ObestcalFoece;
        //    //this.gameObject.transform.position = Vector3.Lerp(transform.position, no, 10 * Time.deltaTime);
        //}
        //if (this.gameObject.transform.position.x <= LevelBound.LeftSideobs)
        //{
        //    new WaitForSeconds(20);
        //    //transform.Translate(Vector3.left * Time.deltaTime * ObestcalFoece);
        //    // no.x= ( Time.deltaTime * ObestcalFoece);
        //    //this.gameObject.transform.position = Vector3.Lerp(transform.position, no, 2 * Time.deltaTime);

        //}
    }
}
